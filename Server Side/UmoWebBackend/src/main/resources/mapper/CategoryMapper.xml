<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ysumly.umowebbackend.mapper.CategoryMapper">

    <select id="findAll" resultType="Category">
        SELECT id, name, slug, parent_id, type, sort_order, created_at, updated_at
        FROM categories
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findByType" resultType="Category">
        SELECT id, name, slug, parent_id, type, sort_order, created_at, updated_at
        FROM categories
        WHERE type = #{type}
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findById" resultType="Category">
        SELECT id, name, slug, parent_id, type, sort_order, created_at, updated_at
        FROM categories
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO categories (name, slug, parent_id, type, sort_order)
        VALUES (#{name}, #{slug}, #{parentId}, #{type}, #{sortOrder})
    </insert>

    <update id="update">
        UPDATE categories
        SET name       = #{name},
            slug       = #{slug},
            parent_id  = #{parentId},
            type       = #{type},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM categories WHERE id = #{id}
    </delete>

    <select id="findByIds" resultType="Category">
        SELECT id, name, slug, parent_id, type, sort_order, created_at, updated_at
        FROM categories
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="countChildren" resultType="long">
        SELECT COUNT(*) FROM categories WHERE parent_id = #{parentId}
    </select>

</mapper>
