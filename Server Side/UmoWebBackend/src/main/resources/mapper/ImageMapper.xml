<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ysumly.umowebbackend.mapper.ImageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO images (original_name, stored_name, path, size,
                            content_type, width, height)
        VALUES (#{originalName}, #{storedName}, #{path}, #{size},
                #{contentType}, #{width}, #{height})
    </insert>

    <select id="findById" resultType="Image">
        SELECT id, original_name, stored_name, path, size,
               content_type, width, height, created_at
        FROM images
        WHERE id = #{id}
    </select>

</mapper>
