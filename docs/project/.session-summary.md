# 会话摘要 - 2026-09-10

## 本次目标

1. 读取项目代码结构并建立项目记忆。
2. 根据当前代码修正 `docs/`。

## 当前阶段

- 后端核心 API 已实现。
- 前端基础设施已实现。
- 前端业务页面大部分仍是占位。
- 文档已按代码基线重新校准，并明确记录已知缺陷。

## 关键决策

| 决策 | 结论 |
|---|---|
| 事实来源 | 当前代码优先于旧设计稿 |
| 正常响应 | 不套 `{ code, data }` |
| 接口数量 | 公开 8 + 管理 19 = 27 |
| 搜索范围 | title + summary，不搜索 Markdown 正文 |
| 分类筛选 | 精确匹配，不包含子分类 |
| 新建状态 | HTTP 200 |
| 前端实现 | Vue 3.5 + Vite 8 + Router 5 + Pinia 3 |
| 前端编辑器 | 未安装 CodeMirror/Monaco |

## 本次文档产物

- 根级 `AGENT.md` 项目规则和记忆。
- 根级 `AGENTS.md` Codex 入口。
- `docs/project/codebase-memory.md` 完整代码基线。
- 重写接口、架构、前端、模块、安全和测试文档。
- 填充 `audit-log.md` 和更新 `CHANGELOG.md`。

## 已验证

- `npm run build`：通过。
- `mvn test`：通过，21 个测试全部通过。

## 待办

| 任务 | 优先级 |
|---|---|
| 修复分类/标签删除保护 | 高 |
| 修复路径穿越风险 | 高 |
| 修复文件与事务一致性 | 中 |
| 增加分页校验 | 中 |
| 实现前端业务页面 | 中 |
| 增加数据库集成测试和前端 E2E | 中 |
| 决定 `app.admin-path` 是否真正生效 | 低 |
