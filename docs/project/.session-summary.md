# 会话摘要 - 阿里云 ECS 公网测试部署

## 本次目标

1. 在单台阿里云 ECS 上部署 MySQL 8.4、Spring Boot 和 Nginx 前端。
2. 处理 ECS 外部仓库访问受限、无域名和安全组边界问题。
3. 验证公网首页、公开 API、管理员登录和容器开机恢复。
4. 将部署事实脱敏同步到仓库，不上传实例标识、地址或任何凭据。

## 当前阶段

- 阿里云 ECS 已运行三个健康容器，宿主 TCP 80 为唯一公网入口。
- 当前无域名，只有 HTTP；安全组和 UFW 已放行 TCP 80。
- 正式内容、备份恢复和本地版本化发布已完成；当前运行正式版 `v1.0.0`，
  Phase 4 独立 Verify、ECS 31/31、迁移、正文索引和访问安全验收通过，正式回滚目标为 rc.8。
- Task 3.1 管理端单文件 Markdown 导入、Task 3.2 分类子级筛选、Task 3.3 图片删除与
  引用保护、Task 3.4 正文全文搜索和 Task 3.5 四款训练游戏已全部合并 master，
  第三阶段出口条件已满足。
- Task 4.1 已完成文章目录、滚动章节高亮、阅读进度和最多 4 篇相关阅读；
  正文内检索由浏览器原生查找承担，不提供站内正文搜索控件。
- Task 4.2 的图片一致性检查已完成：管理端按需扫描断裂引用、数据库记录缺文件和磁盘孤立文件，
  显示来源与扫描时间；不自动修复，也不新增数据库表。
- Task 4.3 已完成：管理端当前页批量分类/标签、归档/恢复，以及 `SCHEDULED` 定时发布；
  调度器按 `Asia/Shanghai` 每 30 秒扫描到期内容，重复执行保持幂等。
- Task 4.4 已完成：文章目录支持 sticky 与半屏抽屉降级，三处 Markdown 编辑工作区等高并在桌面
  双向同步滚动；公开端新增共享工具导航、`/tools` 工具中心和首页工具模块。
- 5.1A 已完成管理端 AI 产品、隐私、接口和字段契约冻结。
- 5.1B 已完成 AI 模式目录后端：两张 MySQL 表、五个默认停用模式、模式 CRUD、复制、
  乐观锁更新、最近 10 版提示词与历史回滚；API 总数由 31 增至 37。
- 5.1C 已完成管理端 AI 设置页；5.1D 已完成设置/能力/转换接口、DeepSeek 适配、
  180 秒超时、单实例请求窗口/并发控制、四类结果校验和脱敏日志；API 总数由 37 增至 40。
- 5.1E 已完成文章 AI 抽屉：正文带入、模式执行/取消、结果编辑/复制/预览、浮动恢复、
  20,000 字符边界、429/502/503/504 提示和浏览器本地恢复；文章正文与 dirty 状态保持独立。

## 关键决策

| 决策 | 结论 |
|---|---|
| 网络入口 | 安全组与 UFW 只放行 TCP 80，MySQL 和后端不暴露 |
| 镜像交付 | ECS 外网受限时由开发机构建并经 SHA-256 校验后 `docker load` |
| 启动方式 | `compose up -d --no-build --wait`，不在 ECS 重新拉取基础镜像 |
| 凭据 | `.env.docker` 权限 600，只保存在 ECS，不进入版本库 |
| 公网状态 | 无域名 HTTP 测试部署，后续再接入 HTTPS |

## 本次文档产物

- 更新 `AGENT.md`、Docker 指南、代码基线记忆、审计日志、变更记录和状态快照。
- 所有仓库内记录均使用脱敏描述，不包含实例 ID、公网 IP、管理路径或凭据。

## 已验证

- 三个 Compose 容器均为 healthy。
- 内部首页、公开 API 和管理员登录均为 HTTP 200。
- 安全组放行后，公网首页为 HTTP 200。
- Docker 服务开机自启，容器策略为 `unless-stopped`。
- 管理员数据库哈希更新后新密码可登录，`token_version` 递增使旧 JWT 失效。

## 后续计划

后续工作已统一整理为 Post-v1 路线，不再在本会话摘要中维护独立待办表：

1. 第一阶段：正式上线与数据安全。
2. 第二阶段：工程交付、质量基线与安全统计，包括最小化访问日志、自动保留策略和自托管报表。
3. 第三阶段：功能已完成并同步 ECS，Markdown 导入、子分类筛选、图片生命周期、
   正文全文搜索和四个训练游戏均已合并且通过 Windows/Linux 与 ECS 验收。
4. 第四阶段：Task 4.1 阅读体验、Task 4.2 图片一致性、Task 4.3 批量管理/定时发布和
   Task 4.4 滚动协同/本地工具入口已完成，不包含文章修订历史。
5. 第五阶段：5.1A 产品契约、5.1B 模式目录后端、5.1C 设置页、5.1D 转换运行时和
   5.1E 文章 AI 抽屉已完成；5.1F 质量发布仍待实施。AI 草稿和结果只保存在浏览器本地，
   不进入数据库或服务器备份。实施入口见
   [`2026-09-18-admin-ai-index.md`](../superpowers/plans/2026-09-18-admin-ai-index.md)。
6. 公开 AI 搜索、原文问答和知识图谱继续后置。

完整顺序、依赖和出口条件见
[`docs/superpowers/plans/2026-09-14-post-v1-roadmap.md`](../superpowers/plans/2026-09-14-post-v1-roadmap.md)。
第一至第三阶段历史记录见
[`docs/superpowers/plans/2026-09-12-four-phase-roadmap.md`](../superpowers/plans/2026-09-12-four-phase-roadmap.md)。
